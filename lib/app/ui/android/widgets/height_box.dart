import 'package:flutter/material.dart';

class HeightBox extends SizedBox {
  const HeightBox(double height, {Key? key}) : super(height: height, key: key);
}

class WidthBox extends SizedBox {
  const WidthBox(double width, {Key? key}) : super(width: width, key: key);
}
